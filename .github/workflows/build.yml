name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore KhaosKode.JEX.LanguageServer.sln

      - name: Build
        run: dotnet build KhaosKode.JEX.LanguageServer.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test KhaosKode.JEX.LanguageServer.sln --configuration Release --no-build --verbosity normal
